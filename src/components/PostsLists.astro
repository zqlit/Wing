---
import { SITE_TITLE, AVATAR_URL } from "../config";
import HeaderLink from "./HeaderLink.astro";
import defineConfig from "../../astro.config.mjs";
import { getCollection } from "astro:content";
import { Image } from "astro:assets";
import FormattedDate from "../components/FormattedDate.astro";
const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

{
  posts.map((post) => (
    <li class="list-none pb-3 border-b border-b-gray-200">
      <a href={`/blog/${post.id}/`} class=" block ">
        <h4 class="title font-bold text-2xl underline decoration-dotted decoration-2 underline-offset-2 pt-8 pb-3">{post.data.title}</h4>
        <p class="date text-sm text-gray-500">
          <FormattedDate date={post.data.pubDate} />
        </p>
      </a>
    </li>
  ))
}
