---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { formatDateKey } from "../utils/dataFormatte";

// 获取所有文章并按日期排序
const allPosts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// 按年份分组
const postsByYear = allPosts.reduce((acc, post) => {
  const year = post.data.pubDate.getFullYear();
  if (!acc[year]) {
    acc[year] = [];
  }
  acc[year].push(post);
  return acc;
}, {} as Record<number, typeof allPosts>);

// 获取年份并排序（从新到旧）
const years = Object.keys(postsByYear).map(Number).sort((a, b) => b - a);
---

<BaseLayout title="归档" description="文章归档">
  <article>
    <h1 class="text-2xl font-bold text-[#111827] dark:text-[#f8fafc] mb-6">归档</h1>
    
    <div class="space-y-8">
      {years.map((year) => (
        <section>
          <h2 class="text-lg font-semibold text-[#111827] dark:text-[#f8fafc] mb-3">{year}</h2>
          <div class="space-y-1.5">
            {postsByYear[year].map((post) => {
              const key = formatDateKey(post.data.pubDate);
              const month = String(post.data.pubDate.getMonth() + 1).padStart(2, '0');
              const day = String(post.data.pubDate.getDate()).padStart(2, '0');
              return (
                <a
                  href={`/${key}.html`}
                  class="flex items-baseline gap-3 text-[#374151] dark:text-[#d1d5db] hover:text-[#111827] dark:hover:text-[#f8fafc] transition-colors"
                >
                  <time class="text-sm text-[#9ca3af] dark:text-[#64748b] min-w-[40px]">
                    {month}/{day}
                  </time>
                  <span class="text-base">
                    {post.data.title}
                  </span>
                </a>
              );
            })}
          </div>
        </section>
      ))}
    </div>
  </article>
</BaseLayout>
